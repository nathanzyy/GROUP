<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Our Group Portfolio</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    /* VIDEO BACKGROUND */
    .video-bg {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: -2;
    }

    /* DARK OVERLAY */
    .overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      z-index: -1;
    }

    /* MAIN CONTENT */
    .content {
      text-align: center;
      color: white;
      margin-top: 15%;
      padding: 20px;
    }

    h1 {
      font-size: 3rem;
      color: #4cc9f0;
      text-shadow: 0 0 15px #4cc9f0;
    }

    p {
      font-size: 1.2rem;
      max-width: 700px;
      margin: 20px auto;
      text-shadow: 0 0 10px black;
    }

    .btn {
      display: inline-block;
      margin-top: 25px;
      padding: 12px 25px;
      background: #f72585;
      color: white;
      font-size: 1.1rem;
      border-radius: 10px;
      text-decoration: none;
      transition: 0.3s;
      box-shadow: 0px 0px 10px #f72585;
    }

    .btn:hover {
      background: #ff4fa3;
      transform: scale(1.08);
    }

  </style>
</head>
<body>

  <!-- LIVE WALLPAPER VIDEO -->
  <video class="video-bg" autoplay muted loop>
    <source src="bg-video.mp4" type="video/mp4">
  </video>

  <!-- DARK OVERLAY FOR READABILITY -->
  <div class="overlay"></div>

  <!-- MAIN CONTENT -->
  <div class="content">
    <h1>Welcome to Our Group Portfolio</h1>
    <p>We are Group 1. This website showcases our teamwork, creativity, and dedication in completing our project. Explore our members and see what makes us unique!</p>

    <a href="members.html" class="btn">Visit Our Members</a>
  </div>

</body>
</html>
